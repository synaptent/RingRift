# Elves Learnings — RingRift improvement epics

Durable, reusable lessons from this run. Promote stable repo truths to docs when appropriate.

- The working tree carries the user's in-flight CMA-ES work (untracked + modified files under
  ai-service and docs/planning). Always stage explicit paths; never `git add -A`.
- `gh repo view` resolves origin (an0mium/RingRift) to synaptent/RingRift — the repo was
  transferred/renamed; gh commands work fine against origin.
- Repo planning docs live in `docs/planning/` with descriptive ALL_CAPS names; research
  write-ups in `docs/research/`.
