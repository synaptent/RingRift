# RingRift Compact Rules (Legacy Shim)

This file is a legacy entrypoint kept for existing links and references.
The authoritative, maintained compact rules live in `docs/rules/COMPACT_RULES.md`.

Canonical rules semantics are defined in `RULES_CANONICAL_SPEC.md` and
implemented by the shared TypeScript engine under `src/shared/engine/**`.
