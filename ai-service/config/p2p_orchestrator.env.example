## RingRift P2P Orchestrator service config (example)
#
# Copy this file to a host-local location (recommended):
#   - Linux/systemd: /etc/ringrift/p2p_orchestrator.env
#   - macOS/launchd: use EnvironmentVariables in the plist
#
# Notes:
# - Do NOT commit real tokens to git.
# - `RINGRIFT_CLUSTER_AUTH_TOKEN` enables auth on all non-GET endpoints.
#

# Required identity
RINGRIFT_NODE_ID=mac-studio
RINGRIFT_PATH=/Users/armand/Development/RingRift

# Network
RINGRIFT_P2P_HOST=0.0.0.0
RINGRIFT_P2P_PORT=8770

# Comma-separated peers (host[:port] or http(s)://host[:port])
# Examples:
#   RINGRIFT_P2P_PEERS=https://cluster.ringrift.ai
#   RINGRIFT_P2P_PEERS=100.107.168.125:8770
RINGRIFT_P2P_PEERS=https://cluster.ringrift.ai

# Optional but recommended if any node is public
# Prefer token files to keep secrets out of env diffs/logs:
#   RINGRIFT_CLUSTER_AUTH_TOKEN_FILE=/etc/ringrift/p2p_orchestrator.token
# Or inline:
#   RINGRIFT_CLUSTER_AUTH_TOKEN=__SET_ME__
RINGRIFT_CLUSTER_AUTH_TOKEN_FILE=/etc/ringrift/p2p_orchestrator.token

# Optional: surfaced in /dashboard + peer heartbeats (defaults to git branch@sha).
# RINGRIFT_BUILD_VERSION=main@deadbeef

# Misc
PYTHONUNBUFFERED=1
