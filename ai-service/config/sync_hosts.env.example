# RingRift Sync Hosts Configuration - EXAMPLE
# Copy this to sync_hosts.env and fill in your actual host details
# Used by scripts/sync_training_data_cron.sh

# Primary host for data sync (required)
# Format: user@host or user@ip
SYNC_PRIMARY_HOST=ubuntu@gpu-primary

# Fallback host (optional - leave empty if not needed)
SYNC_FALLBACK_HOST=ubuntu@gpu-secondary
